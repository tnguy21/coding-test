services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      # Data is gitignored, so it is mounted rather than copied into the image.
      - ./backend/data:/app/data:ro

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
