server {
    listen 80;

    # Same idea as the Vite dev proxy: the browser sees one origin, so no CORS setup is needed.
    location /portfolios {
        proxy_pass http://backend:8000;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
